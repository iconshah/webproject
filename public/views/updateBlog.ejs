<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>List of Blogs</title>
    <link
      rel="icon"
      href="../images/favicon.png"
      height=""
      width=""
      type="image/x-icon"
    />
    <link rel="stylesheet" href="/css/style.css" />
    <!-- External Stylesheet -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <!-- Custom Styles -->
    <link rel="stylesheet" href="../css/style.css" />
    <style>
      body {
        background-color: #000;
      }
    </style>
  </head>

  <body>
    <!-- Navbar -->
    <section id="navBar" class="navbar-fixed-top">
      <%- include('navbar', { user: user }) %>
      <!-- Pass the user variable here -->
    </section>

    <!-- Center Content -->

    <div class="container mt-5 pt-5">
      <h1 class="text-center mb-4 text-white">Update Blog</h1>
      <div class="form-container">
        <form action="/blogs/update/<%= blog.id %>?_method=PUT" method="POST">
          <!-- Note: Use query parameter to specify the desired HTTP method -->
          <div class="form-group">
            <label for="title" class="text-white">Blog Title</label>
            <input
              type="text"
              class="form-control mb-3"
              id="title"
              name="title"
              value="<%= blog.title %>"
              required
            />
          </div>
          <div class="form-group">
            <label for="content" class="text-white">Content</label>
            <textarea
              class="form-control mb-5"
              id="content"
              name="content"
              rows="6"
              required
            >
<%= blog.content %></textarea
            >
          </div>
          <button type="submit" class="btn btn-primary">Update Blog</button>
        </form>
      </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
